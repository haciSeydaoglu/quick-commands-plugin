# Build Plugin

Plugin'i derle ve dağıtılabilir ZIP dosyası oluştur.

## Komut

```bash
./gradlew buildPlugin
```

## Çıktı

Build başarılı olursa `build/distributions/quick-commands-plugin-{version}.zip` dosyası oluşur.

Herhangi bir hata varsa kullanıcıya bildir ve çözüm öner.